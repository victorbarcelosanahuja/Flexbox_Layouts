# 🧩 Práctica Flexbox — Disseny d’interfícies web (M9)

Autor: Víctor Barceló  
Asignatura: M9 — Disseny d’interfícies web  
Descripción: 
Esta práctica reúne 9 ejercicios enfocados en el uso de Flexbox para el diseño y alineación de elementos en páginas web.  
Cada ejercicio desarrolla un concepto fundamental de distribución, alineación o adaptabilidad en interfaces modernas.

---

## Ejercicios

### 1. Alinear elementos en una fila  
Se crea un contenedor Flexbox que muestra tres elementos en una misma fila con un espacio de 10px entre ellos.

### 2. Centrar elementos vertical y horizontalmente  
Flexbox se utiliza para centrar tres elementos en el centro exacto de la pantalla, tanto de forma vertical como horizontal.

### 3. Barra de navegación simple  
Implementación de una barra de navegación horizontal con enlaces distribuidos equitativamente mediante `justify-content: space-around`.

### 4. Flexbox con varias filas  
Uso de `flex-wrap` para organizar seis tarjetas que se ajustan automáticamente en varias filas al reducir el ancho disponible.

### 5. Flex-grow para ajustar tamaños  
Tres columnas con distintos tamaños relativos. La segunda columna crece al doble del tamaño de las otras usando `flex-grow: 2`.

### 6. Justify-content y align-items combinados  
Se combinan ambas propiedades para distribuir los elementos de manera equitativa y centrarlos verticalmente dentro de un contenedor con altura fija.

### 7. Flexbox Grid simple  
Diseño de tipo “grid” con nueve cuadros que se distribuyen en varias filas y columnas mediante `flex-wrap` y `justify-content: center`.

### 8. Alineación personalizada con align-self  
Un contenedor con cuatro elementos, donde uno de ellos usa `align-self` para alinearse de forma distinta al resto.

### 9. Diseño responsive con flex-basis  
Se aplica `flex-basis: 200px` y `flex-wrap` para crear una disposición adaptable a diferentes tamaños de pantalla.

---

## Objetivo general  
Practicar las propiedades esenciales de Flexbox para la creación de interfaces web modernas, centradas en la alineación, distribución y adaptabilidad de los elementos.

---

Autor: Víctor Barceló
